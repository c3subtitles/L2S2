// @flow
import React from 'react';

type Props = {
  params: {
    id: string,
  },
}

type State = {
}

export default class Read extends React.PureComponent {
  props: Props;
  state: State = {};
  componentWillMount() {
  }
  render() {
    return (
      <div />
    );
  }
}
